{
  "version": 2,
  "outputDirectory": "dist/pokedex-angular",
  "buildCommand": "npm install && ng build --configuration=production --verbose --skip-budgets",
  "installCommand": "npm install",
  "framework": "angular"
}
